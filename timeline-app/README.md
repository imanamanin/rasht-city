# Rasht history timeline

React + Framer Motion widget embedded on the homepage (`#history`).

## Develop

```bash
cd timeline-app
npm install
npm run dev
```

## Build (writes to `../assets/timeline/`)

```bash
npm run build
```

Commit the built `assets/timeline/*` files so GitHub Pages serves them without a CI build step.
